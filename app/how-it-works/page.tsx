import Link from "next/link";import {CTA,PageHero} from "@/components/ui";import {Icon} from "@/components/icons";import {steps} from "@/lib/site-data";
export const metadata={title:"How it works"};export default function How(){return <><PageHero eyebrow="How it works" title="A thoughtful process for original work." intro="We add structure to an open-ended journey, while keeping the ideas and ownership with the student."/><section className="section journey">{steps.map((s,i)=><article key={s[0]}><div className="journey-num">{s[0]}</div><div className="journey-copy"><small>{["Application","Matching","Direction","Mentorship","Outcome","Symposium","Application support"][i]}</small><h2>{s[1]}</h2><p>{s[2]}</p>{i===0&&<Link href="/apply" className="text-link">Begin your application <Icon name="arrow"/></Link>}</div><div className={`journey-art art-${i+1}`}><span>{["?","↔","⌁","✦","✓","◎","✉"][i]}</span></div></article>)}</section><section className="soft-section split-copy"><div><h2>What the mentor does</h2><p>A mentor brings domain knowledge, perspective, feedback, and the ability to spot promising directions. They help the student work through uncertainty.</p></div><div><h2>What the student does</h2><p>The student asks, reads, analyzes, tests, revises, and creates. The work remains genuinely theirs—the mentor guides the process, not the answer.</p></div></section><CTA/></>}
