import {CTA,PageHero} from "@/components/ui";import {faqs} from "@/lib/site-data";export const metadata={title:"Frequently asked questions"};export default function FAQ(){return <><PageHero eyebrow="FAQ" title="The practical questions, answered clearly." intro="What we know today—and honest notes where program details are still being finalized."/><section className="section faq-list">{faqs.map((x,i)=><details key={x[0]} open={i===0}><summary><span>{String(i+1).padStart(2,"0")}</span>{x[0]}<b>+</b></summary><p>{x[1]}</p></details>)}</section><CTA title="Still wondering about something?" text="Send us your question. If a detail is still being developed, we will tell you plainly."/></>}
