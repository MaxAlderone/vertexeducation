import Link from "next/link";import {Icon} from "./icons";
export function Eyebrow({children}: {children:React.ReactNode}){return <div className="eyebrow"><span/> {children}</div>}
export function PageHero({eyebrow,title,intro,children}: {eyebrow:string,title:string,intro:string,children?:React.ReactNode}){return <section className="page-hero"><div className="orb orb-a"/><div className="container narrow"><Eyebrow>{eyebrow}</Eyebrow><h1>{title}</h1><p className="lede">{intro}</p>{children}</div></section>}
export function CTA({title="Bring your curiosity. We’ll help with the path.",text="Tell us what you want to explore and we’ll help you understand whether one-on-one research mentorship is the right next step."}:{title?:string,text?:string}){return <section className="cta-wrap"><div className="cta"><div><Eyebrow>Take the next step</Eyebrow><h2>{title}</h2><p>{text}</p></div><div className="cta-actions"><Link className="button cream" href="/apply">Apply as a student <Icon name="arrow"/></Link><Link className="text-link light-link" href="/contact">Ask a question <Icon name="arrow"/></Link></div></div></section>}
export function SectionHead({eyebrow,title,text,link}: {eyebrow:string,title:string,text?:string,link?:[string,string]}){return <div className="section-head"><div><Eyebrow>{eyebrow}</Eyebrow><h2>{title}</h2>{text&&<p>{text}</p>}</div>{link&&<Link className="text-link" href={link[1]}>{link[0]} <Icon name="arrow"/></Link>}</div>}
export function PlaceholderNote(){return <p className="placeholder-note">This page describes our planned offering. Final details are being confirmed and will be shared before enrollment.</p>}
