import Link from "next/link";import {CTA,PageHero} from "@/components/ui";import {Icon} from "@/components/icons";import {subjects} from "@/lib/site-data";
export const metadata={title:"Subjects"};export default function Subjects(){const groups=[...new Set(subjects.map(s=>s.group))];return <><PageHero eyebrow="Subjects" title="Follow the subject that keeps pulling you back." intro="Begin in one field or connect ideas across several. Research often gets interesting at the edges."/><section className="section subject-directory">{groups.map(g=><div key={g}><h2>{g}</h2><div className="subject-list">{subjects.filter(s=>s.group===g).map(s=><Link key={s.name} href={`/subjects/${slug(s.name)}`}><span className={`subject-code tone-${s.tone}`}>{s.code}</span><div><h3>{s.name}</h3><p>{s.desc}</p></div><Icon name="arrow"/></Link>)}</div></div>)}</section><CTA title="Not sure where your question belongs?" text="That is often a good sign. Tell us what you are thinking about and we can help identify the right field and mentor."/></>};function slug(x:string){return x.toLowerCase().replaceAll(" ","-")}
