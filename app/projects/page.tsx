import {CTA,PageHero} from "@/components/ui";import {projects} from "@/lib/site-data";
export const metadata={title:"Example projects"};export default function Projects(){return <><PageHero eyebrow="Student projects" title="A question can become more than an assignment." intro="Explore sample directions across research, data, code, theory, and design. These are ideas—not claims about completed student work."/><section className="section"><div className="notice"><strong>About these examples</strong><p>Until we have student work shared with clear permission, every project below is labeled as an example. We never fabricate student stories or outcomes.</p></div><div className="project-grid full">{projects.map(p=><article className={`project-card tone-${p.tone}`} key={p.title}><div className="project-visual"><span>{p.mark}</span><i/><i/></div><small>Sample project idea · {p.subject}</small><h2>{p.title}</h2><p>{p.type}</p><div className="project-prompt"><b>A possible starting point</b><span>Review what is known, identify a focused gap, and choose a method appropriate to the question.</span></div></article>)}</div></section><CTA title="What could your question become?"/></>}
